// Display order for shop/hotbar category sections. Each item's own category now lives on
// its type definition (js/objects/*.js `category` field) — this file only fixes the order
// they're shown in.

export const CATEGORY_ORDER = ['Flooring', 'Appliances', 'Furniture', 'Farming', 'Ranching', 'Fishing'];
